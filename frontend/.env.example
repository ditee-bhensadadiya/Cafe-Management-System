# In local dev, leave this unset — Vite proxies /api to http://localhost:8000 (see vite.config.js).
# In production (e.g. Vercel), point this at your deployed backend's /api path.
VITE_API_BASE_URL=https://your-backend-host.com/api
